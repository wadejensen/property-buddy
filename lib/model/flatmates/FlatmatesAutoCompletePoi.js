// Will be interesting to see the memory model of how classes are implemented.
// May want to return a JSON object instead but I suspect it already is.

class FlatmatesAutoCompletePoi {
    constructor(name, longTitle, shortTitle, latitude, longitude) {
            this.name = name
            this.longTitle = longTitle
            this.shortTitle = shortTitle
            this.latitude = latitude
            this.longitude = longitude
    }
}